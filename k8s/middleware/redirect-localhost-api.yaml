apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: redirect-localhost-api
  namespace: ecommerce
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`localhost`) && PathPrefix(`/api`)
      kind: Rule
      services:
        - name: backend-service
          port: 8080